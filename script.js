const GOOGLE_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbxjcx5LNp08vORYi8UMEtIxfWbKmaQGkSxQ_J3UYrmT5_8bCHX8Qy--zxijH0sAhvC3Lg/exec";
function showPage(id){document.querySelectorAll('.page').forEach(p=>p.classList.toggle('active',p.id===id));document.querySelectorAll('[data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===id));window.scrollTo({top:0,behavior:'smooth'});}document.querySelectorAll('[data-page]').forEach(b=>b.addEventListener('click',()=>showPage(b.dataset.page)));
function tick(){const end=new Date('2026-09-05T17:30:00+03:00');const diff=Math.max(0,end-new Date());const d=Math.floor(diff/864e5),h=Math.floor(diff%864e5/36e5),m=Math.floor(diff%36e5/6e4),s=Math.floor(diff%6e4/1e3);document.getElementById('countdown').innerHTML=[['Zile',d],['Ore',h],['Minute',m],['Secunde',s]].map(x=>`<div><b>${x[1]}</b><span>${x[0]}</span></div>`).join('')}tick();setInterval(tick,1000);
document.getElementById('rsvpForm').addEventListener('submit',async(e)=>{e.preventDefault();const status=document.getElementById('formStatus');status.textContent='Se trimite...';const data=Object.fromEntries(new FormData(e.target).entries());data.data_trimiterii=new Date().toLocaleString('ro-RO');try{if(GOOGLE_SCRIPT_URL.includes('PASTEAZA')) throw new Error('Lipsește linkul Google Apps Script.');await fetch(GOOGLE_SCRIPT_URL,{method:'POST',mode:'no-cors',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});status.textContent='Mulțumim! Confirmarea a fost trimisă.';e.target.reset();}catch(err){status.textContent='Formularul este pregătit, dar trebuie introdus linkul Google Sheets în script.js.';}});
